<%= form_for(current_user.favoring_relationships.find_by(favorited_id: @recipe.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "unfavorite", class: "btn" %>
<% end %>